# News App

Android application built with Jetpack Compose, Navigation, MVVM, and Clean Architecture. The app fetches US top headlines from NewsAPI with category filtering, search, pull-to-refresh, and article details.

Replace `YOUR_API_KEY` in `AppModule` with a valid News API key.
